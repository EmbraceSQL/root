

## Requirements

You are going to need:

* NodeJs >= 18
* PostgreSQL >= 14

These examples assume you are running locally with your shell able to connect
to your PostgreSQL with:

```shell
psql
```

Create a new database `dvdrental` on your local PostgeSQL server.

```shell
curl "https://embracesql.github.io/dvdrental.sql" | psql
```