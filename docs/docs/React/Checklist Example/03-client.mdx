import HtmlSource from "!!raw-loader!../../../../samples/checklist/index.html";
import MainSource from "!!raw-loader!../../../../samples/checklist/src/client/main.tsx";
import AppSource from "!!raw-loader!../../../../samples/checklist/src/client/app.tsx";
import ChecklistSource from "!!raw-loader!../../../../samples/checklist/src/client/checklists.tsx";
import CodeBlock from "@theme/CodeBlock";

# Create Client

Now we'll start in the React UI, let's get all the support code in place.

## Steps

### Get MUI

In our app directory, we need to add a few packages to get the MUI toolkit,
which we'll be using for a grid user interface.

```shell
npm install @mui/material @emotion/react @emotion/styled @mui/x-data-grid @mui/icons-material
```

### Create App HTML

We're making a single page app, so here is the single page.

<CodeBlock language="html" title="index.html">
  {HtmlSource}
</CodeBlock>

### Create a React Main

Create the application root as shown. This will be loaded by `index.html`.

<CodeBlock language="tsx" title="./src/client/main.tsx">
  {MainSource}
</CodeBlock>

### Create a React App

A very simple app at the root.

<CodeBlock language="tsx" title="./src/client/app.tsx">
  {AppSource}
</CodeBlock>
