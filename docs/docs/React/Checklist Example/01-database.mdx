# Create a Database

First thing you will need a database.

## Steps

### Have Postgres

This tutorial assumes you have a local PostgreSQL installed.

### Create a Database

We've provided a handy database create script for you, which will
create a local database called `checklist`.

```shell
curl "https://embracesql.github.io/checklist.sql" | psql
```