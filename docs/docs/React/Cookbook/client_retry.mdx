import ReactSource from "!!raw-loader!../../../../samples/next/src/app/client_retry/page.tsx";
import CodeBlock from "@theme/CodeBlock";

# Client Retry

Network interruptions? Turn on retry!


## Requirements

Make sure you are read as specified in the [Cookbook](./).

## Steps

### Generate Code


### Code React

When you set up the `EmbraceSQLClient` just set the `retries`.

<CodeBlock language="tsx" title="src/app/client_retry/page.tsx" >
  {ReactSource}
</CodeBlock>