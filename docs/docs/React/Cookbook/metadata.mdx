import ReactSource from "!!raw-loader!../../../../samples/next/src/app/metadata/page.tsx";
import CodeBlock from "@theme/CodeBlock";

# Database Metadata

Say you want to browse the schema, or explore database metadata about
tables, columns and indexes.

EmbraceSQL generates the metadata for you -- no need to query the system
catalogs.


## Requirements

Make sure you are read as specified in the [Cookbook](./).

## Steps


### Code React

The `Metadata` is an exported object as a constant. No need to even hook.

<CodeBlock language="tsx" title="src/app/by_index/page.tsx" >
  {ReactSource}
</CodeBlock>